import { Seller } from './seller';

export class Product {
    id: number;
    name: string;
    category: string;
    imagePath: string;
    seller: number;

    constructor(id, name, category, imagePath, seller) {
        id;
        name;
        category;
        imagePath;
        seller;
    }
}
