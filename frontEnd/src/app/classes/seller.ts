import { Product } from './product';

export class Seller {
  id: number;
  name: string;
  category: string;
  imagePath: string;

  constructor(id, name, category, imagePath) {
    id;
    name;
    category;
    imagePath;
  }
}
