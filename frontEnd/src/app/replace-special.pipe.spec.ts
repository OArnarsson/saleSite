import { ReplaceSpecialPipe } from './replace-special.pipe';

describe('ReplaceSpecialPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceSpecialPipe();
    expect(pipe).toBeTruthy();
  });
});
